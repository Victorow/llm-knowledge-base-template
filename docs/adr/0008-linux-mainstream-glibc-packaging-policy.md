# Linux mainstream glibc packaging policy

The first Linux desktop release will target mainstream glibc-based desktop distributions using PyInstaller plus a tarball/installer script, XDG autostart, an internal Background Agent scheduler, and tray support where available. Alpine/musl, NixOS-specific packaging, headless/minimal distributions, and guaranteed tray behavior across every desktop environment are out of scope for V1.
