# Manual memory through daily logs

The first desktop UI will not include direct editing of compiled Knowledge Articles. Users can inspect articles, open them externally, and add Manual Memory entries to Daily Logs; Compile then incorporates that source material so frontmatter, sources, wikilinks, index rows, and build logs stay LLM-owned and structurally consistent.
