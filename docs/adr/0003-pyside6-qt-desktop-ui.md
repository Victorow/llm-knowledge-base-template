# PySide6 Qt desktop UI

The control panel will use PySide6/Qt as the primary desktop UI toolkit. This keeps the application Python-first, gives native system tray support on Windows and Linux, works with PyInstaller packaging, and avoids the extra runtime and integration complexity of Electron, Tauri, or a local web server for the first desktop product.
