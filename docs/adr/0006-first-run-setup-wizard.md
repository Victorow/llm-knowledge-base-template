# First-run setup wizard

The desktop application will include a First-Run Setup Wizard instead of relying on README-only manual setup. The wizard will validate prerequisites, select the KB location and Agent Backend, install or repair Claude Code and Codex hooks, configure user-session autostart and daily compile automation, and run smoke tests so users can make the pipeline operational without editing JSON or scheduler files by hand.
