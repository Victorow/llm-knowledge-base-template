# Multiple KB profiles with single active capture target

The desktop app will support multiple named KB Profiles, each pointing to its own KB Data directory and settings, but only one Active KB Profile will be targeted by Capture Hooks and automatic background capture at a time. This supports personal/project/client separation without introducing multi-destination hook routing complexity in V1.
